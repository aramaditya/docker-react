# version: '3'
# services:
#     prod:
#         build:
#             context: .
#             dockerfile: Dockerfile
#         ports:
#             - "8082:80"

services:
    web: 
        environment:
            - CHOKIDAR_USEPOLLING=true
        build: 
            context: .
            dockerfile: Dockerfile.dev
        # context: .
        ports:
            - "3001:3000"
        volumes:
            - /app/node_modules
            - .:/app
    tests:
        build:
            context: .
            dockerfile: Dockerfile.dev
        
        volumes:
                - /app/node_modules
                - .:/app
            
        command: ["npm", "run", "test"]


        